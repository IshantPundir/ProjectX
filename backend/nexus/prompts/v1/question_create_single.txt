You are drafting a single new interview question for a structured, AI-led VOICE interview.
The recruiter has described what they want; produce a question that fits the stage's
purpose, aligns with the JD signals, and does not duplicate questions already in this
or prior stages' banks.

The question is read ALOUD to the candidate, who hears it ONCE and answers by SPEAKING.
Apply the same spoken constraints as the full bank-generation system:

  - ONE ask. Exactly one thing. No "and… and…", no comma-spliced topic lists. Depth
    belongs in follow-ups, not in a multi-part lead.
  - Self-contained. Answerable on first hearing. A scenario must be inline — never a bare
    "which would you pick?" with no context. The engine reads the text verbatim and cannot
    improvise missing context.
  - Sayable. ≤240 characters, conversational register, numbers spelled in words ("five
    years", not "5 yrs").

Signal binding: `proposed_signal_probed` must be the exact signal value string from the
snapshot — verbatim, no paraphrase. For a one-of / "or" signal, ask option-agnostically
so any valid option is credited.

`proposed_mandatory`: true ONLY when the question verifies a KNOCKOUT signal. Every other
question — including required, non-knockout signals — is false.

`proposed_position`: where in the bank to insert the new question. Default to appending
after the last question (highest position + 1) unless the recruiter's instruction
specifies otherwise.

Do NOT duplicate any question already listed in the existing bank or prior banks.

# JD signals (current snapshot)
{signals_json}

# This stage
- name: {stage_name}
- type: {stage_type}
- difficulty: {stage_difficulty}
- duration_minutes: {stage_duration_minutes}
- signal_filter.include_types: {signal_filter_types}
- pass_criteria: {pass_criteria_json}

# This stage's existing bank (do NOT duplicate)
{existing_bank_json}

# Prior stages' banks (do NOT duplicate)
{prior_banks_json}

# Recruiter instruction
{instruction}

# Output
Return JSON with exactly these five fields:
{
  "proposed_text": "...",
  "proposed_signal_probed": "<must be one of signal_filter.include_types signal values>",
  "proposed_mandatory": true/false,
  "proposed_position": <integer; append at end of stage by default>,
  "rationale": "one-sentence explanation of how this question addresses the instruction"
}
